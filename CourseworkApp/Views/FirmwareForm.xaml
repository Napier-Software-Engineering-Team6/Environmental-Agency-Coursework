<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CourseworkApp.ViewModels"
             xmlns:converters="clr-namespace:CourseworkApp.Converters"
             x:Class="CourseworkApp.Views.FirmwareForm"
             Title="Edit Firmware"
             x:DataType="viewmodels:FirmwareFormViewModel">

    <ContentPage.Resources>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter"/>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20"
                             Spacing="15">

            <Label Text="Edit Firmware Configuration"
                   FontSize="Large"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <HorizontalStackLayout Spacing="10">
                <Label Text="Firmware ID:"
                       VerticalOptions="Center"
                       FontAttributes="Bold"/>
                <Label Text="{Binding FirmwareId}"
                       VerticalOptions="Center"/>
            </HorizontalStackLayout>

            <HorizontalStackLayout Spacing="10">
                <Label Text="Sensor Type:"
                       VerticalOptions="Center"
                       FontAttributes="Bold"/>
                <Label Text="{Binding SensorType}"
                       VerticalOptions="Center"/>
            </HorizontalStackLayout>

            <Label Text="Firmware Version (e.g., 1.0.0):"
                   FontAttributes="Bold"/>
            <Entry Text="{Binding FirmwareVersion}"
                   Keyboard="Default"
                   Placeholder="X.Y.Z"/>

            <Label Text="Release Date:"
                   FontAttributes="Bold"/>
            <DatePicker Date="{Binding ReleaseDate}"
                        Format="yyyy-MM-dd"/>
            <Label Text="End of Life Date:"
                   FontAttributes="Bold"/>
            <DatePicker Date="{Binding EndofLifeDate}"
                        Format="yyyy-MM-dd"/>
            <HorizontalStackLayout Spacing="10"
                                   VerticalOptions="Center">
                <Label Text="Is Active:"
                       VerticalOptions="Center"
                       FontAttributes="Bold"/>
                <Switch IsToggled="{Binding IsActive}"
                        VerticalOptions="Center"/>
            </HorizontalStackLayout>

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"/>

            <Grid ColumnDefinitions="*,*"
                  ColumnSpacing="10"
                  Margin="0,20,0,0">
                <Button Grid.Column="0"
                        Text="Save"
                        Command="{Binding SubmitCommand}"/>
                <Button Grid.Column="1"
                        Text="Cancel"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="Gray"/>
            </Grid>

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center"
                               VerticalOptions="CenterAndExpand"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>